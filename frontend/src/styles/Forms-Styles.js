import styled from 'styled-components';
import { Col, Row } from 'react-bootstrap';

export const ActRow = styled(Row)`
    margin-bottom: 10px;
`;

export const ActColLeft = styled(Col)`
    text-align: right;
`;

export const ActColRight = styled(Col)`
    text-align: left;
`;